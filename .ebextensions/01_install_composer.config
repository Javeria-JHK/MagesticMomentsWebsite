files:
  "/opt/elasticbeanstalk/hooks/appdeploy/pre/10_install_composer.sh":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/bash
      cd /var/app/staging
      composer install --no-dev --prefer-dist
      php artisan key:generate
      php artisan migrate --force

